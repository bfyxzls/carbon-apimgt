# carbon/sp列表
* 服务名规则：用户UUID_应用UUID_PRODUCTION
  * 09c54439-6201-4922-823e-326a1e6dc15f_fa7a2660-f110-4207-99f3-f3f591302eee_PRODUCTION

# 通过应用来找到应用对应的服务名称，以便在carbon/sp中配置这个服务
1. am_application表，通过应用UUID找到subscriber_ID
2. am_subscriber表，通过subscriber_ID找到user_id
3. 将user_id和应用ID拼接，得到服务名称

# apim-analytics-publisher项目中analytics.publisher.client包
* 审计日志项目完成kafka对接`org.wso2.am.analytics.publisher.client_1.2.23.jar`

# 默认应用名称
* org.wso2.carbon.apimgt.impl.APIConstants.DEFAULT_APPLICATION_NAME 改成 默认应用
* org.wso2.carbon.apimgt.impl.APIConstants.DEFAULT_APPLICATION_DESCRIPTION 改成 系统为您创建的默认应用，可快速开始使用北大法宝MCP服务
* 删除应用sql的报错
  * org.wso2.carbon.apimgt.impl.dao.constants.SQLConstants.GET_PAGINATED_SUBSCRIBED_APIS_BY_APP_ID_SQL添加相关字段

# 订阅表am_subscription添加createdTime和updatedTime字段
添加订阅接口扩展属性，devportal-api.yaml字段的更新，`api#am#devportal.war`
* org.wso2.carbon.apimgt.rest.api.gateway
    * src\gen\java\org\wso2\carbon\apimgt\rest\api\gateway\dto\SubscriptionDTO.java
* org.wso2.carbon.apimgt.rest.api.store.v1
    * /src/main/resources/devportal-api.yaml
      * createdTime
      * updatedTime
    * java\org\wso2\carbon\apimgt\rest\api\store\v1\mappings\SubscriptionMappingUtil.java
     * fromSubscriptionToDTO()方法添加createdTime和updatedTime
* org.wso2.carbon.apimgt.impl.dao.ApiMgtDAO.initSubscribedAPI()
  * subscribedAPI.setCreatedTime(resultSet.getString("SUB_CREATED_TIME"));
  * subscribedAPI.setUpdatedTime(resultSet.getString("SUB_UPDATED_TIME"));

# 订阅事件添加attributes字段
* 在自定义的扩展项目`apim-custom-extension`项目中添加的，它订阅了商城平台的消息`order-settlement-completed-apim-topic`，当用户下单成功后，会触发这个事件，从而调用APIM的订阅接口，完成API的订阅操作
* com.pkulaw.mcpapimes.listener.OrderListener.create()
* 调用apim订阅接口：api/am/devportal/subscriptions，完成了扩展字段attributes的传递

# api策略报错添加try...catch
* org.wso2.carbon.apimgt.rest.api.publisher.v1.common.mappings.OperationPolicyMappingUtil.fromOperationPolicySpecAttributesToDTO() 添加了try...catch操作

# 审记日志添加扩展字段
* org.wso2.carbon.apimgt.gateway.handlers.analytics.SynapseAnalyticsDataProvider.getProperties()
  方法，添加所需要的字段在properties属性里

# mcp请求体添加handler
* 添加apiCategory为MCP,分来标记它是MCP服务，当api在publisher平台被标记为MCP分类后，需要deploy才能让MCPInitHandler对它生效
* org.wso2.carbon.apimgt.gateway.handlers.mcp.MCPInitHandler
* 在org.wso2.carbon.apimgt.rest.api.publisher.v1.common.TemplateBuilderUtil类型中注册它
* McpInitHandler方法buildMCPRequest()的json序列化统一为GSON
* 在[org.wso2.carbon.apimgt.api]和[org.wso2.carbon.apimgt.gateway]分别添加了mcp的方法支持
* 最后在涉及日志[SynapseAnalyticsDataProvider.java](components/apimgt/org.wso2.carbon.apimgt.gateway/src/main/java/org/wso2/carbon/apimgt/gateway/handlers/analytics/SynapseAnalyticsDataProvider.java)中添加扩展字段
  * mcpMethod
  * mcpHttpMethod
  * mcpApiElectedResource
  * mcpNoAuthRequest

# mcp服务添加频次过滤
  * 非tools/call请求不记次
  * 添加相关判断逻辑 [ThrottleHandler.java](components/apimgt/org.wso2.carbon.apimgt.gateway/src/main/java/org/wso2/carbon/apimgt/gateway/handlers/throttling/ThrottleHandler.java)

