# WSO2 APIM 生成OAuth Keys时consumer_key为空的根本原因分析

## 问题描述

在通过第三方IDP登录WSO2系统后，调用生成应用的OAuth keys接口时返回500错误（错误码900967），并且`am_application_key_mapping`表中的`consumer_key`字段为空。

## 错误堆栈分析

从错误堆栈可以看出，问题发生在以下调用链：

1. `ApplicationsApiServiceImpl.applicationsApplicationIdGenerateKeysPost` (API入口)
2. `APIConsumerImpl.requestApprovalForApplicationRegistration`
3. `ApplicationRegistrationSimpleWorkflowExecutor.complete`
4. `AbstractApplicationRegistrationWorkflowExecutor.dogenerateKeysForApplication`
5. `AMDefaultKeyManagerImpl.createApplication` ← **这里失败，返回500错误**
6. `ApplicationUtils.updateOAuthAppAssociation`
7. `ApiMgtDAO.updateApplicationKeyTypeMapping`

## 根本原因

### 1. 执行流程

在`ApplicationRegistrationSimpleWorkflowExecutor.complete`方法中：

```java
// 第76行：首先创建注册记录和key mapping记录（此时CONSUMER_KEY为NULL）
dao.createApplicationRegistrationEntry((ApplicationRegistrationWorkflowDTO)workFlowDTO,false);

// 第77行：然后生成keys
generateKeysForApplication(regWFDTO);
```

### 2. createApplicationRegistrationEntry方法

在`ApiMgtDAO.createApplicationRegistrationEntry`方法中（第301-308行），创建`am_application_key_mapping`记录时：

```java
ps = conn.prepareStatement(keyMappingEntry);
ps.setInt(1, application.getId());
ps.setString(2, dto.getKeyType());
ps.setString(3, dto.getStatus().toString());
ps.setString(4, dto.getKeyManager());
ps.setString(5, UUID.randomUUID().toString());
ps.setString(6, APIConstants.OAuthAppMode.CREATED.name());
ps.execute();
```

对应的SQL语句（`ADD_APPLICATION_KEY_MAPPING_SQL`）：
```sql
INSERT INTO AM_APPLICATION_KEY_MAPPING (APPLICATION_ID,KEY_TYPE,STATE,KEY_MANAGER,UUID,CREATE_MODE) 
VALUES(?,?,?,?,?,?)
```

**注意：这个INSERT语句不包含CONSUMER_KEY字段，所以记录创建时CONSUMER_KEY为NULL。**

### 3. Key Manager创建OAuth应用失败

在`AbstractApplicationRegistrationWorkflowExecutor.dogenerateKeysForApplication`方法中（第153行）：

```java
OAuthApplicationInfo oAuthApplication = keyManager.createApplication(workflowDTO.getAppInfoDTO());
```

在`AMDefaultKeyManagerImpl.createApplication`方法中（第163行），调用DCR客户端创建OAuth应用：

```java
createdClient = dcrClient.createApplication(request);
```

**这里返回了500错误**，导致：
- `KeyManagerClientException`被抛出
- `oAuthApplication`可能为null或者没有正确的`consumer_key`

### 4. updateApplicationKeyTypeMapping方法

在`ApplicationUtils.updateOAuthAppAssociation`方法中（第156-157行）：

```java
application.addOAuthApp(keyType,keyManagerName,oAuthApplication);
ApiMgtDAO.getInstance().updateApplicationKeyTypeMapping(application,keyType,keyManagerName);
```

在`ApiMgtDAO.updateApplicationKeyTypeMapping`方法中（第2405-2435行）：

```java
OAuthApplicationInfo app = application.getOAuthApp(keyType, keyManagerId);
String consumerKey = null;
if (app != null) {
    consumerKey = app.getClientId();
}

if (consumerKey != null && application.getId() != -1) {
    Connection connection = null;
    PreparedStatement ps = null;
    try {
        connection = APIMgtDBUtil.getConnection();
        connection.setAutoCommit(false);
        ps = connection.prepareStatement(addApplicationKeyMapping);
        ps.setString(1, consumerKey);
        // ... 设置其他参数
        ps.executeUpdate();
        connection.commit();
    } catch (SQLException e) {
        handleException("Error updating the CONSUMER KEY...", e);
        // ⚠️ 注意：这里没有执行rollback！
    } finally {
        APIMgtDBUtil.closeAllConnections(ps, connection, null);
    }
}
```

**关键问题分析：**

1. **如果`consumerKey`为null**：UPDATE语句不会执行，导致CONSUMER_KEY字段保持为NULL。

2. **如果UPDATE执行时出错**：
   - SQLException被捕获并重新抛出
   - **但是没有执行rollback**（虽然设置了`setAutoCommit(false)`）
   - 这可能导致事务处于未提交状态，但异常被抛出后连接被关闭

3. **如果OAuth应用创建成功但更新失败**：
   - Key Manager端可能已经创建了OAuth应用（consumer_key已生成）
   - 但数据库更新失败，导致`am_application_key_mapping`表中的CONSUMER_KEY为空
   - 这会造成数据不一致：Key Manager中有OAuth应用，但APIM数据库中consumer_key为空

## 为什么Key Manager返回500错误？

从错误信息看：
```
Caused by: org.wso2.carbon.apimgt.impl.kmclient.KeyManagerClientException: Received status code: 500 Reason: 
```

可能的原因：

1. **用户权限问题**：通过第三方IDP登录的用户可能没有在Key Manager中创建OAuth应用的权限
   - 错误信息显示尝试创建的应用名：`eeabc635-98a1-428d-9b73-6b89737e0ac7_0e38e61a-48fc-4acf-90fd-e058c555e52c_PRODUCTION`
   - 这个应用名包含了用户ID和应用程序UUID

2. **Key Manager服务配置问题**：
   - DCR端点配置不正确
   - Key Manager服务不可用
   - 网络连接问题

3. **用户上下文问题**：
   - 通过第三方IDP登录的用户，在Key Manager的上下文中可能没有正确的用户信息
   - 需要检查`OAUTH_CLIENT_USERNAME`参数是否正确传递

## 可能的场景分析

根据错误堆栈和代码分析，有两种可能的情况：

### 场景1：OAuth应用创建完全失败
- `dcrClient.createApplication(request)` 返回500错误
- `KeyManagerClientException`被抛出
- `oAuthApplication`为null或没有consumer_key
- `updateOAuthAppAssociation`可能没有被调用，或者`consumerKey`为null
- 结果：`am_application_key_mapping`表中的CONSUMER_KEY为空

### 场景2：OAuth应用创建成功，但更新数据库失败（用户提到的可能性）
- Key Manager端成功创建了OAuth应用（consumer_key已生成）
- `buildDTOFromClientInfo`成功设置了consumer_key
- 但在`updateApplicationKeyTypeMapping`执行UPDATE时出错
- 可能的原因：
  - 数据库连接问题
  - 事务冲突
  - 约束违反（虽然不太可能，因为记录已存在）
- 结果：Key Manager中有OAuth应用，但`am_application_key_mapping`表中的CONSUMER_KEY为空

**要确认是哪种场景，需要：**
1. 检查Key Manager的日志，看OAuth应用是否真的被创建
2. 检查数据库日志，看是否有UPDATE语句执行失败的记录
3. 检查`updateApplicationKeyTypeMapping`方法是否有异常被捕获但没有正确处理

## 解决方案建议

### 1. 检查Key Manager日志

查看Key Manager服务的日志，找出返回500错误的具体原因。可能的问题：
- 用户不存在
- 权限不足
- 服务配置错误

### 2. 检查用户权限

确保通过第三方IDP登录的用户在Key Manager中有创建OAuth应用的权限。可能需要：
- 在Key Manager中创建对应的用户
- 授予用户必要的权限

### 3. 检查AMDefaultKeyManagerImpl配置

查看`AMDefaultKeyManagerImpl`的配置，特别是：
- `kmAdminAsAppOwner`配置：如果设置为true，会使用Key Manager管理员作为应用所有者
- DCR客户端配置：确保DCR端点URL正确

### 4. 改进错误处理

建议在`AbstractApplicationRegistrationWorkflowExecutor.dogenerateKeysForApplication`方法中：
- 检查`oAuthApplication`是否为null
- 检查`consumer_key`是否为空
- 如果创建失败，应该回滚已创建的记录或标记为失败状态

### 5. 临时解决方案

如果OAuth应用创建失败，可以：
1. 手动清理`am_application_key_mapping`表中的空记录
2. 检查Key Manager服务状态
3. 重新尝试生成keys

## 相关代码位置

1. `ApplicationRegistrationSimpleWorkflowExecutor.complete` (第65-84行)
2. `ApiMgtDAO.createApplicationRegistrationEntry` (第248-326行)
3. `AbstractApplicationRegistrationWorkflowExecutor.dogenerateKeysForApplication` (第124-167行)
4. `AMDefaultKeyManagerImpl.createApplication` (第112-177行)
5. `ApiMgtDAO.updateApplicationKeyTypeMapping` (第2402-2432行)

## 总结

根据代码分析和错误堆栈，问题的根本原因可能是：

### 最可能的情况：
1. `am_application_key_mapping`记录在OAuth应用创建之前就被创建了（CONSUMER_KEY为NULL）
2. Key Manager创建OAuth应用失败（返回500错误），导致没有consumer_key
3. 后续的UPDATE操作因为consumer_key为null而不执行，导致CONSUMER_KEY字段保持为NULL

### 需要进一步排查的情况（用户提到的可能性）：
1. OAuth应用在Key Manager端创建成功（consumer_key已生成）
2. 但在更新`am_application_key_mapping`表时出错
3. `updateApplicationKeyTypeMapping`方法的异常处理可能有问题（没有rollback）

## 建议的排查步骤

1. **检查Key Manager日志**：
   - 查看OAuth应用是否真的被创建
   - 查看返回500错误的具体原因
   - 检查是否有consumer_key生成

2. **检查数据库日志**：
   - 查看是否有UPDATE语句执行
   - 查看是否有SQLException
   - 检查事务状态

3. **检查`updateApplicationKeyTypeMapping`方法**：
   - 确认是否有异常被捕获但没有正确处理
   - 确认是否需要添加rollback逻辑

4. **检查用户权限**：
   - 确认通过第三方IDP登录的用户在Key Manager中有创建OAuth应用的权限
   - 检查`OAUTH_CLIENT_USERNAME`参数是否正确传递

5. **改进错误处理**（建议）：
   - 在`updateApplicationKeyTypeMapping`方法中添加rollback逻辑
   - 在`dogenerateKeysForApplication`方法中，如果OAuth应用创建失败，应该清理已创建的记录或标记为失败状态
